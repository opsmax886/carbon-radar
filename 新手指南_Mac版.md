# 碳雷达 · Mac 新手部署指南(手把手版)

> 这份指南假设你**完全不懂编程**。我会写清楚每一步点哪里。
> 全程约 30 分钟,**不需要输入任何命令**,只有 3 处需要"复制粘贴",我会用 🔴 标出来。
>
> 看不懂的词直接跳过,照着做就行。

---

## 开始之前:先搞懂 3 个词(30 秒)

| 词 | 通俗解释 |
|---|---|
| **GitHub 仓库** | 一个网盘文件夹。你的程序放这里,云端才能每天自动帮你干活。 |
| **GitHub Actions** | GitHub 免费送的"定时机器人"。你设好时间,它每天自动帮你跑程序。 |
| **Secrets(密码本)** | 放密码的地方。你的 API 密钥放这里,不会泄露到代码里。 |

**你要注册 3 个账号:**

1. **DeepSeek** —— 负责给每条信息写摘要(要花一点点钱,约每月 1-5 元)
2. **钉钉** —— 负责把结果推送到你手机
3. **GitHub** —— 负责每天定时自动运行

> 💡 不想花钱?**可以跳过第 1 步**。系统会自动降级成"规则打分",功能照样完整,
> 只是摘要会比较朴素(直接截取标题)。以后想加随时可以补。

---

## 第 1 步:申请 DeepSeek API Key(约 5 分钟)

1. 打开浏览器,访问 👉 **https://platform.deepseek.com/**
2. 点右上角「**Sign up / 注册**」,用**手机号**注册并登录
3. 登录后,点左侧菜单的「**充值**」(或 "Top up"),按提示充**少量金额**
   (10 元左右就够这个系统跑好几个月)
4. 点左侧菜单的「**API keys**」
5. 点「**创建 API key**」→ 随便起个名字,比如 `碳雷达` → 确定
6. 🔴 **屏幕上会出现一串以 `sk-` 开头的字符,这就是密钥。**
   **它只显示这一次!** 请立刻做两件事:
   - 点「复制」按钮
   - 粘贴到一个安全的地方(比如 Mac 自带的「备忘录」App,新建一条笔记存起来)

> ⚠️ **重要提醒**:这串 `sk-` 开头的密钥等于你的钱包,**不要发给任何人**,
> 也不要在微信/群里截图。存好后继续下一步,一会儿要用。

---

## 第 2 步:建钉钉群机器人(约 5 分钟)

### 2.1 先准备一个群

如果你已经有钉钉群(公司群、项目群),可以**直接用**,但注意:机器人发的内容**全群都能看到**。
更推荐建一个**只有你自己的群**:

1. 打开钉钉 → 点右上角「**+**」→「**发起群聊**」
2. 选「**面对面建群**」→ 随便输入 4 个数字(比如 8888)→ 进入
3. 这样你就有了一个只有自己的群,命名为「双碳情报」

### 2.2 添加机器人

1. 进入这个群 → 点右上角「**⋯**」(三个点)→「**群设置**」
2. 找到「**智能群助手**」→ 点「**添加机器人**」
3. 选择「**自定义**(通过 Webhook 接入)」→ 点「添加」
4. 机器人名字填:**碳雷达**
5. 往下找到「**安全设置**」,勾选「**加签**」(这样最安全)
6. 勾选后会显示两样东西,**都要复制保存到备忘录**:
   - 🔴 **Webhook 地址**:`https://oapi.dingtalk.com/robot/send?access_token=......`
   - 🔴 **密钥**:`SEC......`(点旁边的「复制」)
7. 点「**完成**」

> 💡 如果你觉得"加签"麻烦,也可以勾「**自定义关键词**」,关键词填 **碳雷达**。
> 这样就不需要保存密钥了(但第 5 步少填一个也能用)。

---

## 第 3 步:注册 GitHub + 安装 GitHub Desktop(约 5 分钟)

### 3.1 注册 GitHub

1. 访问 👉 **https://github.com/signup**
2. 用**邮箱**注册(建议用常用邮箱),设置用户名和密码
3. 完成邮箱验证

### 3.2 安装 GitHub Desktop(图形化工具,不用敲命令)

1. 访问 👉 **https://desktop.github.com/**
2. 点「**Download for macOS**」
3. 下载完成后,在「下载」文件夹找到它,双击打开
4. 会弹出一个小窗口,把左侧的 **GitHub Desktop 图标拖到右侧的 Applications 文件夹**
5. 打开「启动台」或「应用程序」,找到 **GitHub Desktop** 双击运行
6. 首次打开会问是否打开来自互联网的 App → 点「**打开**」
7. 点「**Sign in to GitHub.com**」→ 在浏览器里登录你刚注册的账号 → 授权

---

## 第 4 步:把代码传到 GitHub(约 3 分钟)

好消息:**我已经帮你把代码打包好了**,这一步很简单。

### 4.1 找到代码文件夹

你的代码在这个位置(可以直接复制这一行):

```
/Users/luoyanghe/Documents/Deepseek/carbon-radar
```

**怎么打开它?**
- 方法一:打开「访达(Finder)」→ 左侧点「文稿」→ 进入 `Deepseek` 文件夹 → 你会看到 `carbon-radar` 文件夹
- 方法二:按 `Command + 空格` 打开搜索 → 输入 `终端` → 回车 → 粘贴这行再回车:
  ```
  open /Users/luoyanghe/Documents/Deepseek/carbon-radar
  ```

### 4.2 用 GitHub Desktop 上传

1. 打开 **GitHub Desktop**
2. 顶部菜单点「**File**」→「**Add Local Repository...**」
3. 点「**Choose...**」按钮
4. 在弹出的窗口里找到并选中 `carbon-radar` 这个文件夹 → 点「**Open**」
5. 回到窗口,点「**Add Repository**」
6. 现在你应该能看到一个文件列表(说明代码已就位)
7. 点窗口**顶部中间**的蓝色按钮「**Publish repository**」
8. 弹窗里填:
   - **Name**:`carbon-radar`(保持默认即可)
   - **Description**:可留空
   - ⚠️ **「Keep this code private」这个勾选框**:
     - **想用手机上网页看板 → 把勾去掉**(代码公开,但免费版只有这样才支持网页)
     - **在意隐私 → 保持勾选**(不要网页,只看钉钉推送)
9. 点「**Publish repository**」,等几秒

✅ 完成后,用浏览器打开 `https://github.com/你的用户名/carbon-radar`,能看到文件就成功了。

---

## 第 5 步:填 3 个密码(约 3 分钟)

这一步是把你第 1、2 步保存的密钥告诉 GitHub。

1. 浏览器打开你的仓库页面 `https://github.com/你的用户名/carbon-radar`
2. 点页面顶部菜单的「**Settings**」(齿轮图标)

> ⚠️ 注意是**仓库的** Settings(在仓库页面里),不是右上角你头像旁边的那个。

3. 左侧菜单往下找,点「**Secrets and variables**」→「**Actions**」
4. 点绿色按钮「**New repository secret**」
5. 依次添加下面 3 条(**注意 Name 必须一模一样**,不能有空格):

| Name(照着填) | Secret(粘贴你保存的内容) |
|---|---|
| `DEEPSEEK_API_KEY` | 第 1 步那串 `sk-` 开头的 |
| `DINGTALK_WEBHOOK` | 第 2 步的 Webhook 地址 |
| `DINGTALK_SECRET` | 第 2 步的 `SEC` 开头的密钥 |

> 如果你第 2 步选的是「自定义关键词」而不是「加签」,第 3 条就不用填。

**操作方式**:每次都是 → 点「New repository secret」→ Name 框填左边那列 → Secret 框粘贴右边那列
→ 点最下方「**Add secret**」→ 加完一条再加下一条。

---

## 第 6 步:打开开关 + 手动跑一次(约 2 分钟)

1. 在仓库页面,点顶部菜单的「**Actions**」
2. 如果看到蓝色提示条写着 workflows,点「**I understand my workflows, go ahead and enable them**」
3. 左侧列表点「**碳雷达每日更新**」
4. 右侧会出现一个「**Run workflow**」下拉按钮 → 点它
5. 在弹出的绿色按钮「**Run workflow**」上再点一次
6. **刷新页面**(按 `Command + R`),你会看到一条记录出现,前面有个**黄色小圆点**(表示正在运行)

⏳ 等 **1-3 分钟**,黄点变成**绿色对勾**就成功了。

> 💡 **你会看到两个任务,这是正常的:**
> - 「**update**」= 抓取+推送,这个必须有绿色对勾 ✅
> - 「**deploy**」= 发布网页,如果你还没做第 8 步、或者仓库是私有的,它会显示
>   灰色(跳过)或黄色警告 —— **这都不影响使用**,只要 update 是绿的就行。

---

## 第 7 步:确认成功(看两个地方)

### ✅ 检查一:钉钉有没有收到消息

打开你第 2 步建的那个群,应该收到一条「**碳雷达 · 2026-XX-XX**」的消息,
里面有「🎯 重点标讯」「📜 政策与方法学」等板块,点标题能跳转到原文。

### ✅ 检查二:GitHub 有没有自动提交数据

回到仓库页面刷新,你会看到 `web` 和 `data` 文件夹里的文件**更新时间变成了刚刚**,
说明数据已自动生成。

**如果两个都没成功**,看下面的「出问题了怎么办」。

> ⏰ 之后系统会**每天早上 9:12 自动运行**,不需要你做任何事。
> Mac 关机、没联网都不影响 —— 它在 GitHub 的云端跑。

---

## 第 8 步(可选,推荐):开通网页看板

**前提:第 4 步时你把「Keep this code private」的勾去掉了(仓库是公开的)。**

1. 仓库页面 →「**Settings**」→ 左侧菜单找到「**Pages**」
2. 在「**Source**」下拉框里选「**GitHub Actions**」→ 点「**Save**」
3. 回到「**Actions**」页面 → 左侧点「碳雷达每日更新」→「**Run workflow**」再跑一次
4. 等绿色对勾后,访问这个网址(把"你的用户名"换成你的):

```
https://你的用户名.github.io/carbon-radar/
```

🎉 这就是你的完整看板:可以搜索、按地区分组、按业务线筛选、只看今日新增。

**强烈建议**:在 Mac 的 Safari 里打开后,点「分享」按钮 →「**添加到程序坞**」;
手机上也可以「添加到主屏幕」,以后像 App 一样一点就开。

---

## 第 9 步:把微信公众号的文章收进来(手动,但很简单)

**微信公众号是国内唯一没法自动抓的来源**(微信不开放文章列表接口,这是平台限制,写多少程序都绕不过去)。

但**单篇文章是能抓的** —— 所以办法是:**你贴链接,系统自动补全其余信息。**

### 怎么用

1. 微信里打开一篇文章 → 点右上角「**⋯**」→「**复制链接**」
2. 浏览器打开这个地址:

   ```
   https://github.com/opsmax886/carbon-radar/blob/main/config/manual_links.txt
   ```

3. 点右上角的「**铅笔图标**」✏️(编辑)
4. 光标移到**最后一行下面**,粘贴链接(一行一个)
5. 点页面底部绿色的「**Commit changes**」

**完成!** 第二天早上 9 点,系统会自动:

- 抓取标题、公众号名、发布时间
- 判断归类(含"招标/采购/申报"→归到招投标)
- 算业务匹配度、生成 AI 摘要
- 并入网页看板和钉钉推送

### 可选:想指定归类

在链接后面加个标签就行(不加也能自动判断):

```
https://mp.weixin.qq.com/s/xxxxx #tender
```

| 标签 | 归到 |
|---|---|
| `#tender` | 招投标 |
| `#policy` | 政策法规 |
| `#methodology` | 方法学 / 标准 |
| `#news` | 市场动态 |

### 两个说明

- **贴过的链接不用删**,系统记住了,不会重复抓
- 想让**同事也帮忙投稿**:把上面那个网址发给他们,有仓库权限的就能加

> 💡 微信文章发布后有延迟,**当天贴、次日收到**。如果你希望更快,可以手动跑一次工作流(见第 6 步)。

---

## 出问题了怎么办?

### ❌ Actions 里是红色叉号

**先看是哪一个任务红了:**

- 如果红的是「**deploy**」→ **可以忽略**。那是发布网页用的,只要你还没开通第 8 步,
  或者仓库是私有的,它失败是正常的。看「**update**」是不是绿的就行。
- 如果红的是「**update**」→ 点进那条红色记录 → 点左侧「**update**」→ 展开看哪一步红了。

| 报错关键词 | 原因 | 解决办法 |
|---|---|---|
| `DEEPSEEK_API_KEY` 相关 | 密钥名打错或没填 | 回第 5 步,注意 Name 必须完全一致 |
| `钉钉推送失败` / `errcode` 不是 0 | Webhook 填错,或加签密钥不对 | 回第 2 步重新复制;确认安全设置选的是「加签」 |
| `YAML` 报错 | 配置文件被误改 | 告诉我,我帮你修 |

> 💡 附注:其实**即使没配 DeepSeek 密钥,系统也能正常跑**,只会用规则打分兜底。
> 所以红色叉号如果是 DeepSeek 引起的,先检查密钥名有没有拼错。

### ❌ 钉钉没收到消息,但 Actions 是绿的

- 检查钉钉机器人的**安全设置**是否和 Secrets 一致(选了「加签」就必须填 `DINGTALK_SECRET`)
- 检查群消息是不是被你静音了 → 群设置 → 消息免打扰,关掉

### ❌ 一个标讯都没有

先看 Actions 日志里有没有「✓ 抓取成功」。如果显示「最新内容停在 X 月」,
说明是**那个网站本身停更了**——不是你的系统坏了。系统会在钉钉消息里提醒你。

### ❌ 想改推送时间

默认**早上 9:12**。要改的话告诉我你想几点,我帮你改。
(这个文件里有个"UTC 时区"的坑,自己改容易改错:UTC 时间 = 北京时间 − 8 小时。)

### ❌ 程序卡住 / 想重新来过

在 GitHub Desktop 里可以看历史、回退版本。或者直接告诉我你看到什么,
我来帮你判断。

---

## 部署完成检查清单

- [ ] DeepSeek 密钥已保存到备忘录(或决定跳过)
- [ ] 钉钉群机器人已建好,Webhook(和密钥)已保存
- [ ] GitHub 账号已注册,代码已 Publish 到仓库
- [ ] 3 个 Secrets 已填好(Name 拼写完全一致)
- [ ] Actions 手动跑过一次,显示绿色对勾
- [ ] 钉钉收到「碳雷达」消息
- [ ] (可选)网页看板能打开

---

## 最后:一件比部署更重要的事

系统跑起来后,**真正决定它好不好用的是 `config/keywords.yml` 这个文件**。

- **发现漏掉的信息** → 告诉我那个说法,我帮你加进关键词
- **发现不想看的信息** → 告诉我,我帮你加进排除词

头一两周请留意钉钉推送的内容,把"漏的"和"多的"都告诉我,我帮你调。

---

## 需要我帮忙?

你可以直接告诉我:

- 「我卡在第 X 步,屏幕上是这样……(截图或描述)」
- 「这个标讯不该出现,帮我屏蔽掉」
- 「我关注 XX 省,帮我只看这个省」
- 「推送时间改成晚上 8 点」

我会直接帮你改好文件,你只需要在 GitHub Desktop 里点一下同步。
